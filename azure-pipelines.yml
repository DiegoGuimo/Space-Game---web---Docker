trigger:
- '*'

variables:
  buildConfiguration: 'Release'
  webRepository: 'tailspin-web'  # Nombre de la imagen en ACR
  tag: '$(Build.BuildId)'
  wwwrootDir: 'Tailspin.SpaceGame.Web/wwwroot'
  dotnetSdkVersion: '8.x'
  containerRegistry: 'tailspinspacegame26514.azurecr.io'  # Nombre correcto del ACR
  azureSubscription: 'Resource Manager - Tailspin - Space Game'  # Conexión a Azure RM

stages:
- stage: 'Build'
  displayName: 'Build the web application'
  jobs: 
  - job: 'Build'
    displayName: 'Build job'
    pool:
      name: RetoAzure
      demands:
      - agent.name -equals MyAgent

    steps:
    - task: Docker@2
      displayName: 'Build and push the image to ACR'
      inputs:
        command: buildAndPush
        repository: $(containerRegistry)/$(webRepository)  # Se usa la URL del ACR
        dockerfile: '$(Build.SourcesDirectory)/Tailspin.SpaceGame.Web/Dockerfile'
        containerRegistry: 'Resource Manager - Tailspin - Space Game'  # Nombre exacto en DevOps
        tags: |
          $(tag)

- stage: 'Deploy'
  displayName: 'Deploy the container'
  dependsOn: Build
  jobs:
  - job: 'Deploy'
    displayName: 'Deploy job'
    pool:
      name: RetoAzure
      demands:
      - agent.name -equals MyAgent
    variables:
    - group: Release  # 🔹 Importa la Library "Release"
    steps:
    - task: AzureWebAppContainer@1
      displayName: 'Deploy to Azure App Service'
      inputs:
        azureSubscription: 'Resource Manager - Tailspin - Space Game'  # Nombre correcto de la conexión
        appName: '$(WebAppName)'  # Variable desde la Library
        containers: '$(containerRegistry)/$(webRepository):$(Build.BuildId)'
